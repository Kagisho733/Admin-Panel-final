import { FaBoxOpen, FaCopy, FaTimes } from "react-icons/fa";
import toast from "react-hot-toast";
import type { CustomerActivityRecord } from "../../services/activityService";
import { formatCurrency } from "../../utils/formatCurrency";
export default function ActivityDetailsModal({record,title,onClose}: {record:CustomerActivityRecord;title:string;onClose:()=>void}){const copy=async()=>{await navigator.clipboard.writeText(record.customer.uid);toast.success("Customer UID copied")};return <div className="fixed inset-0 z-50 flex items-center justify-center bg-slate-950/70 p-3 backdrop-blur-sm" onMouseDown={e=>{if(e.target===e.currentTarget)onClose()}}><div className="flex max-h-[90vh] w-full max-w-3xl flex-col overflow-hidden rounded-3xl bg-white shadow-2xl"><header className="flex justify-between bg-gradient-to-r from-slate-950 to-blue-950 p-6 text-white"><div><p className="text-xs font-bold uppercase tracking-[.18em] text-cyan-300">Customer activity</p><h2 className="mt-1 text-2xl font-black">{title}</h2><p className="mt-1 text-sm text-slate-300">{record.customer.displayName} • {record.customer.email}</p></div><button onClick={onClose} className="grid h-10 w-10 place-items-center rounded-xl hover:bg-white/10" aria-label="Close details"><FaTimes/></button></header><div className="flex-1 overflow-y-auto bg-slate-50 p-6"><div className="mb-5 flex flex-wrap gap-3"><div className="rounded-xl bg-white p-4 shadow-sm"><p className="text-xs uppercase text-slate-500">Items</p><p className="text-2xl font-black">{record.itemCount}</p></div><div className="rounded-xl bg-white p-4 shadow-sm"><p className="text-xs uppercase text-slate-500">Value</p><p className="text-2xl font-black">{formatCurrency(record.subtotal)}</p></div></div>{record.items.length===0?<div className="grid place-items-center gap-3 rounded-2xl bg-white p-12 text-slate-500"><FaBoxOpen size={32}/><p>No products are currently saved.</p></div>:<div className="space-y-3">{record.items.map((item,index)=><div key={`${item.productId}-${index}`} className="flex items-center gap-4 rounded-2xl border bg-white p-4"><div className="grid h-16 w-16 place-items-center overflow-hidden rounded-xl bg-slate-100">{item.product.imageUrl?<img src={item.product.imageUrl} alt="" className="h-full w-full object-cover"/>:<FaBoxOpen className="text-slate-400"/>}</div><div className="min-w-0 flex-1"><p className="truncate font-bold">{item.product.name}</p><p className="text-xs text-slate-500">SKU: {item.product.sku||"Not set"}</p></div><div className="text-right"><p className="font-bold">{formatCurrency(item.lineTotal||item.product.price)}</p><p className="text-xs text-slate-500">Qty {item.quantity}</p></div></div>)}</div>}</div><footer className="flex items-center justify-between border-t p-4"><button onClick={()=>void copy()} className="flex items-center gap-2 text-xs font-bold text-slate-500"><FaCopy/>Copy technical UID</button><button onClick={onClose} className="rounded-xl bg-slate-900 px-5 py-2.5 font-bold text-white">Close</button></footer></div></div>}
