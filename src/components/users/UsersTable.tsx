import { FaEdit, FaEye, FaSort, FaSortDown, FaSortUp, FaTrash } from "react-icons/fa";
import type { User } from "../../types/User";
import { formatCurrency } from "../../utils/formatCurrency";
type SortField = "firstName" | "email" | "role" | "totalOrders" | "totalSpent";
interface Props {users:User[];onView:(user:User)=>void;onEdit:(user:User)=>void;onDelete?:(user:User)=>void;sortField:SortField;sortDirection:"asc"|"desc";setSortField:React.Dispatch<React.SetStateAction<SortField>>;setSortDirection:React.Dispatch<React.SetStateAction<"asc"|"desc">>}
export default function UsersTable({users,onView,onEdit,onDelete,sortField,sortDirection,setSortField,setSortDirection}:Props){const sort=(field:SortField)=>{if(sortField===field)setSortDirection(value=>value==="asc"?"desc":"asc");else{setSortField(field);setSortDirection("asc")}};const SortIcon=({field}:{field:SortField})=><span className="ml-2 inline-flex text-slate-400">{sortField===field?sortDirection==="asc"?<FaSortUp/>:<FaSortDown/>:<FaSort/>}</span>;const heading=(field:SortField,label:string)=><button onClick={()=>sort(field)} className="inline-flex items-center font-bold">{label}<SortIcon field={field}/></button>;return <div className="overflow-x-auto rounded-2xl border border-slate-200 bg-white shadow-sm"><table className="min-w-full"><thead className="bg-slate-50"><tr><th>{heading("firstName","User")}</th><th>{heading("email","Email")}</th><th>{heading("role","Role")}</th><th>{heading("totalOrders","Orders")}</th><th>{heading("totalSpent","Paid spending")}</th><th><span className="sr-only">Actions</span></th></tr></thead><tbody>{users.length===0?<tr><td colSpan={6} className="py-14 text-center text-slate-500">No users match the current filters.</td></tr>:users.map(user=><tr key={user.id}><td><div className="flex items-center gap-3"><span className="grid h-10 w-10 shrink-0 place-items-center overflow-hidden rounded-xl bg-gradient-to-br from-blue-500 to-indigo-700 font-black text-white">{user.avatar?<img src={user.avatar} alt="" className="h-full w-full object-cover"/>:`${user.firstName?.[0]||""}${user.lastName?.[0]||""}`||"U"}</span><div><p className="font-bold text-slate-900">{`${user.firstName} ${user.lastName}`.trim()||"Unnamed user"}</p><p className="text-xs text-slate-500">{user.phone||"No phone number"}</p></div></div></td><td>{user.email}</td><td><span className={`rounded-full px-3 py-1 text-xs font-bold capitalize ${user.role==="admin"?"bg-violet-100 text-violet-700":"bg-blue-100 text-blue-700"}`}>{user.role}</span></td><td className="font-bold">{user.totalOrders}</td><td className="font-bold text-emerald-700">{formatCurrency(user.totalSpent)}</td><td><div className="flex justify-end gap-2"><button onClick={()=>onView(user)} className="grid h-9 w-9 place-items-center rounded-lg border border-sky-200 bg-sky-50 text-sky-700 hover:bg-sky-100" title="View user" aria-label={`View ${user.firstName}`}><FaEye/></button><button onClick={()=>onEdit(user)} className="grid h-9 w-9 place-items-center rounded-lg border border-amber-200 bg-amber-50 text-amber-700 hover:bg-amber-100" title="Edit role" aria-label={`Edit ${user.firstName}`}><FaEdit/></button>{onDelete&&<button onClick={()=>onDelete(user)} className="grid h-9 w-9 place-items-center rounded-lg border border-red-200 bg-red-50 text-red-700 hover:bg-red-100" title="Delete user" aria-label={`Delete ${user.firstName}`}><FaTrash/></button>}</div></td></tr>)}</tbody></table></div>}
