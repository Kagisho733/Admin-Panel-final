import { FaBoxOpen,FaCopy,FaStar,FaTimes,FaTrash } from "react-icons/fa";
import toast from "react-hot-toast";
import type { ReviewRecord } from "../../services/reviewService";
const date=(value:string)=>new Intl.DateTimeFormat("en-ZA",{dateStyle:"long",timeStyle:"short"}).format(new Date(value));
export default function ReviewDetailsModal({review,onClose,onDelete}:{review:ReviewRecord;onClose:()=>void;onDelete:()=>void}){const copy=async()=>{await navigator.clipboard.writeText(review.customer.uid);toast.success("Customer UID copied")};return <div className="fixed inset-0 z-50 flex items-center justify-center bg-slate-950/70 p-3 backdrop-blur-sm" onMouseDown={e=>{if(e.target===e.currentTarget)onClose()}}><div role="dialog" aria-modal="true" className="w-full max-w-2xl overflow-hidden rounded-3xl bg-white shadow-2xl"><header className="flex justify-between bg-gradient-to-r from-slate-950 to-blue-950 p-6 text-white"><div><p className="text-xs font-bold uppercase tracking-[.18em] text-cyan-300">Customer feedback</p><h2 className="mt-1 text-2xl font-black">Review details</h2></div><button onClick={onClose} className="grid h-10 w-10 place-items-center rounded-xl hover:bg-white/10" aria-label="Close review"><FaTimes/></button></header><div className="space-y-5 bg-slate-50 p-6"><div className="flex items-center gap-4 rounded-2xl border bg-white p-4">{review.product.imageUrl?<img src={review.product.imageUrl} alt="" className="h-16 w-16 rounded-xl object-cover"/>:<span className="grid h-16 w-16 place-items-center rounded-xl bg-slate-100 text-slate-400"><FaBoxOpen/></span>}<div><p className="font-black">{review.product.name}</p><p className="text-xs text-slate-500">SKU: {review.product.sku||"Not set"}</p></div></div><div className="rounded-2xl border bg-white p-5"><div className="flex items-center justify-between"><div><p className="font-bold">{review.customer.displayName}</p><p className="text-sm text-slate-500">{review.customer.email||"Email unavailable"}</p></div><div className="flex gap-1 text-amber-400">{Array.from({length:5}).map((_,i)=><FaStar key={i} className={i<review.rating?"":"text-slate-200"}/>)}</div></div><blockquote className="mt-5 border-l-4 border-blue-500 pl-4 text-lg leading-8 text-slate-700">“{review.comment}”</blockquote><p className="mt-4 text-xs text-slate-500">Submitted {date(review.createdAt)}</p></div></div><footer className="flex flex-col-reverse gap-3 border-t p-4 sm:flex-row sm:items-center sm:justify-between"><button onClick={()=>void copy()} className="flex items-center gap-2 text-xs font-bold text-slate-500"><FaCopy/>Copy technical UID</button><div className="flex gap-3"><button onClick={onClose} className="rounded-xl border px-4 py-2.5 font-bold">Close</button><button onClick={onDelete} className="flex items-center gap-2 rounded-xl bg-red-600 px-4 py-2.5 font-bold text-white"><FaTrash/>Delete review</button></div></footer></div></div>}
