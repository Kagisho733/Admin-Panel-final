# Firebase Functions backend used by the admin panel.
VITE_API_URL=https://us-central1-FIREBASE_PROJECT_ID.cloudfunctions.net/api

# Legacy Firebase/Supabase variables are intentionally omitted. Core admin
# operations must go through the authenticated backend API.
